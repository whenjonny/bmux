// Sources/Wea/WeaWorkspaceManager.swift
import Foundation
import os

/// Placeholder for future workspace UI features.
/// The bridge now uses one-shot subprocess mode (no terminal panels needed).
@MainActor
final class WeaWorkspaceManager {
    private let logger = Logger(subsystem: "com.cmuxterm.app", category: "WeaWorkspaceManager")

    init() {}
}
